// 3. Напишите функцию, которая принимает строку и возвращает ее, но все буквы переведены
// в верхний регистр.

const str = 'hello'

const doStrToUpperCase = () => str.toUpperCase();
console.log(doStrToUpperCase());